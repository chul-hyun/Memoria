define(['module/Classer'], function (Classer) {

    var PathCollection = Classer({
        Init: 
    })


    return {
        getConstructor: getConstructor,
        join: join,
        isPath: isPath
    }
});