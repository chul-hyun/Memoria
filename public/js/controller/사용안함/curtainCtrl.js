
//공통 컨트롤러 설정 - 모든 컨트롤러에서 공통적으로 사용하는 부분들 선언
define(['LazyRegister'], function (LazyRegister) {
    //컨트롤러 선언
    LazyRegister.controller('curtainCtrl', ['$scope', '$route', function ($scope, $route) {
    } ]);
});